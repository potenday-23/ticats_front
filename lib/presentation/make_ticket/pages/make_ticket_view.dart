import 'package:flutter/material.dart';

class MakeTicketView extends StatelessWidget {
  const MakeTicketView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
